spring:
  application:
    name: eureka-service
  cloud:
      config:
        uri: http://config-server:8888
        fail-fast: true
        retry:
          maxAttempts: 20
          max-interval: 6000
          initial-interval: 6000

