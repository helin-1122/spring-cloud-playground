spring:
  application:
    name: reservation-service
  cloud:
      config:
        uri: http://config-server:8888
        fail-fast: true
        retry:
          max-attempts: 20
          max-interval: 6000
          initial-interval: 6000

